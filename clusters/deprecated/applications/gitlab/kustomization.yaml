apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: gitlab
resources:
  - HelmRepository.yaml
  - HelmRelease.yaml
configMapGenerator:
  - name: gitlab-values
    files:
      - values.yaml=Values.yaml
configurations:
  - KustomizeConfig.yaml