apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: homepage
  namespace: homepage
spec:
  entryPoints:
    - websecure
  routes:
  - match: Host(`homepage.app.homelab.com`) && PathPrefix(`/`)
    kind: Rule
    services:
    - kind: Service
      name: homepage
      port: 3000
      sticky:
        cookie:
          httpOnly: true
          secure: true
          sameSite: none
  tls:
    secretName: homepage-tls
