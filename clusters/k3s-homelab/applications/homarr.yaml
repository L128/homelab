apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: homarr
  namespace: myapps
spec:
  interval: 5m
  chart:
    spec:
      chart: homarr
      version: ">=1.0.0"
      sourceRef:
        kind: HelmRepository
        name: homarr
        namespace: flux-system
      interval: 1h
  valuesFrom:
  - kind: ConfigMap
    name: homarr-values
    valuesKey: values.yaml