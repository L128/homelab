apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: traefik
resources:
  - HelmRepository.yaml
  - HelmRelease.yaml
configMapGenerator:
  - name: traefik-values
    files:
      - values.yaml=Values.yaml
configurations:
  - KustomizeConfig.yaml