kind: Kustomization
namespace: monitoring
resources:
  - HelmRelease.yaml
configMapGenerator:
  - name: loki-mixin-values
    files:
      - values.yaml=Values.yaml
configurations:
  - KustomizeConfig.yaml