kind: Kustomization
namespace: monitoring
resources:
  - HelmRelease.yaml
configMapGenerator:
  - name: k8s-monitoring-values
    files:
      - values.yaml=Values.yaml
configurations:
  - KustomizeConfig.yaml