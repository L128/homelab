apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: loki-ingress-tls
  namespace: monitoring
spec:
  entryPoints:
    - websecure
    # - web
  routes:
  - match: Host(`loki.app.homelab.me`) && PathPrefix(`/`)
    kind: Rule
    services:
    - name: loki
      port: 3100
  tls:
    secretName: monitoring-tls