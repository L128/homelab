tempo:
  storage:
    trace:
      backend: s3
      s3:
        endpoint: minio:9000
        bucket: tempo
        insecure: true
persistence:
  enabled: true
  storageClassName: "longhorn"
  accessModes:
    - ReadWriteOnce
  size: 10Gi
